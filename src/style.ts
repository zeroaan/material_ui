import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  button: {
    width: 150,
    margin: "auto",
    marginBottom: 10,
    display: "flex",
  },
  buttonGroup: {
    width: 100,
    height: 50,
    borderRadius: 30,
  },
});
